apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: broadcaster-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Asadullah378/DevOps-With-Kubernetes
    path: Broadcaster/manifests/overlays/staging
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

